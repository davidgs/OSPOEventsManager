  password: undefined

}

PostgreSQL connection pool and Drizzle ORM initialized

Testing PostgreSQL connection...

Using DatabaseStorage (PostgreSQL) for data storage

Using Keycloak service at: http://keycloak:8080⁠

Setting up Keycloak proxy to internal URL: http://keycloak:8080⁠

Using PostgreSQL session store

Using custom Keycloak URL: http://keycloak:8080/auth/⁠

Attempting to initialize database tables...

Initializing database tables (if they don't exist)...

✅ PostgreSQL connection successful

✅ Database connection test successful

Current events table columns: [

  { column_name: 'id', data_type: 'integer' },

  { column_name: 'name', data_type: 'text' },

  { column_name: 'description', data_type: 'text' },

  { column_name: 'location', data_type: 'text' },

  { column_name: 'start_date', data_type: 'date' },

  { column_name: 'end_date', data_type: 'date' },

  { column_name: 'website', data_type: 'text' },

  { column_name: 'type', data_type: 'text' },

  { column_name: 'goals', data_type: 'ARRAY' },

  { column_name: 'status', data_type: 'text' },

  {

    column_name: 'created_at',

    data_type: 'timestamp without time zone'

  },

  {

    column_name: 'updated_at',

    data_type: 'timestamp without time zone'

  },

  { column_name: 'link', data_type: 'text' },

  { column_name: 'priority', data_type: 'text' },

  { column_name: 'goal', data_type: 'ARRAY' }

]

✅ Users table initialized

✅ Added link column to events table if missing

✅ Added priority column to events table if missing

✅ Added created_at column to events table if missing

✅ Added updated_at column to events table if missing

✅ Added goal column to events table if missing

✅ Added cfp_deadline column to events table if missing

✅ Added type column to events table if missing

✅ Added status column to events table if missing

✅ Events table initialized

✅ CFP submissions table initialized

✅ Attendees table initialized

✅ Sponsorships table initialized

✅ Assets table initialized

✅ Stakeholders table initialized

✅ Approval workflows table initialized

✅ Workflow reviewers table initialized

✅ Workflow stakeholders table initialized

✅ Workflow comments table initialized

✅ Workflow history table initialized

✅ Sessions table initialized

✅ Database initialization completed successfully!

Database tables initialized successfully