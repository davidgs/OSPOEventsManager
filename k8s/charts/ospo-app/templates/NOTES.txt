Thank you for installing {{ .Chart.Name }}.

Your OSPO Event Management application has been deployed to your Kubernetes cluster.

{{- if .Values.ingress.enabled }}
{{- range $host := .Values.ingress.hosts }}
  {{- range .paths }}
  The application is available at: http{{ if $.Values.ingress.tls }}s{{ end }}://{{ $host.host }}{{ .path }}
  {{- end }}
{{- end }}

{{- else }}
To access the application, you can port-forward the service to your local machine:

  kubectl port-forward service/{{ .Release.Name }}-app {{ .Values.appServer.service.port }}:{{ .Values.appServer.service.port }}

Then access the application at: http://localhost:{{ .Values.appServer.service.port }}
{{- end }}

Administrator credentials:

Keycloak Admin:
  Username: {{ .Values.keycloak.auth.adminUser }}
  Password: [stored in secret {{ template "ospo-app.keycloak.secretName" . }}]
  
  To get the password: kubectl get secret {{ template "ospo-app.keycloak.secretName" . }} -o jsonpath="{.data.{{ .Values.keycloak.auth.secretKeys.adminPasswordKey }}}" | base64 --decode

Database:
  Username: {{ .Values.postgresql.auth.username }}
  Database: {{ .Values.postgresql.auth.database }}
  Password: [stored in secret {{ template "ospo-app.postgresql.secretName" . }}]
  
  To get the password: kubectl get secret {{ template "ospo-app.postgresql.secretName" . }} -o jsonpath="{.data.{{ .Values.postgresql.auth.secretKeys.userPasswordKey }}}" | base64 --decode

MinIO Object Storage:
  Username: {{ .Values.minio.auth.rootUser }}
  Password: [stored in secret {{ template "ospo-app.minio.secretName" . }}]
  
  To get the password: kubectl get secret {{ template "ospo-app.minio.secretName" . }} -o jsonpath="{.data.{{ .Values.minio.auth.secretKeys.rootPasswordKey }}}" | base64 --decode